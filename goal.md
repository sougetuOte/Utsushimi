# goal.md — Phase 0「抽出」

**状態：承認済み（2026-09-23 主人）**
種別：feature（G0 あり）。材料は `docs/handoff/phase0-brief.md`。
前身 = `C:\work5\Kage-Shiki`（以下 `K/`）。基準コミット（着手前の Utsushimi）= `8d6f970`。

## 完了条件

1. `docs/concept.md`・`docs/behaviors.md`・`docs/lessons.md` の3本がある。
2. **出典**：3本とも、`##` の節ごとに少なくとも1つ、`behaviors.md` は項目（表の行）ごとに少なくとも1つ、
   `K/<パス>:<行>` または `K/<パス>:<開始>-<終了>` の形の出典が付いている。出典は前身の実ファイルの該当行と内容が一致する。
3. **振る舞い**：前身の US-1〜US-19 が**全件** `behaviors.md` にあり、各項目に 優先度（Must / Should / Could）と
   前身での状況（実装済み／未統合／未着手）が付いている。開発者向けの US は「開発者向け」と印を付け、`lessons.md` へ送ってよい。
4. **保護指定**：`CLAUDE.md` の保護指定1〜4 が、それぞれ `behaviors.md` の少なくとも1項目に「保護指定N」として紐付いている。
5. **キャラ体系**：C1〜C11・S1〜S7・L1〜L6 の全24項目が、中身つきで（番号と名前だけでなく）`concept.md` にある。権限マトリクスもある。
6. **基本設計**：`concept.md` に、記憶（前身 01）・人格と欲求（02）・自分で調べる（05）・GUI とボディ（06）・キャラ（07）の節があり、
   完成形（1対1の相棒。AITuber は拡張口）と、前身の Phase 1〜4 を相棒として整理し直した道のりがある。
7. **教訓**：`lessons.md` に hotfix の Bug-1〜7、教訓 L-1〜L-5、宿題 B-7〜B-11 が全件あり、各項目に「完了条件：」で始まる行が
   **実起動で確かめる形**（「テストが通った」ではなく「実起動して〜できた」）で付いている。
8. **技術選定を確定していない**：前身の技術（Python・tkinter・SQLite 等）への言及は、すべて「前身では」と明示した文脈にある。

## 検証方法

検収役が見る。採点範囲は**本フェーズの成果コミット**（`8d6f970` より後で、報告に列挙したもの）に限る。

| # | 完了条件 | 確かめ方 |
|---|---|---|
| V1 | 1 | `ls docs/concept.md docs/behaviors.md docs/lessons.md` |
| V2 | 2 | 出典の形を正規表現で数え、`##` 節数・`behaviors.md` の表の行数と突き合わせる。**加えて出典を任意に5件以上選び、`K/` の該当行を読んで一致を確かめる** |
| V3 | 3 | `grep -oE 'US-[0-9]+' docs/behaviors.md \| sort -u` が19件。各 US 行に優先度と状況の語がある |
| V4 | 4 | `grep -c '保護指定[1-4]'` で1〜4がそれぞれ1件以上 |
| V5 | 5 | `grep -oE '\b[CSL][0-9]{1,2}\b' docs/concept.md \| sort -u` が24件。任意の3項目を `K/docs/memos/middle-draft/07-character-design.md` と読み比べ、中身が落ちていないか見る |
| V6 | 6 | 見出しを読んで、節の有無を確かめる |
| V7 | 7 | `Bug-[1-7]`・`L-[1-5]`・`B-(7\|8\|9\|10\|11)` が `lessons.md` に全件。`完了条件：` の行数が17以上。任意の3件が「実起動」の形か読む |
| V8 | 8 | `grep -nE 'Python\|tkinter\|SQLite'` の各ヒットの前後を読み、「前身では」の文脈にあるか見る |
| V9 | やらないこと | 成果コミットの変更ファイルが `docs/*.md`（と承認済みの `goal.md`）だけ：`git diff --name-only 8d6f970..<最後の成果コミット>` |
| V10 | やらないこと | 前身に書き込んでいない：`git -C C:\work5\Kage-Shiki rev-parse --short HEAD` が `39b030f`、かつ `git -C … status --porcelain \| sha1sum` が `cd492486c02d63037a55b9ef7a161276397b9b78`（着手前の控え。2026-09-23 取得。M 10行・?? 2行） |
| V11 | 品質 | 3本に制御文字 `[\x00-\x08\x0b\x0c\x0e-\x1f]` が無い |

V10 は前身の主人が作業ツリーを触ると偽になる。**検収は成果コミット直後に行う。**

## やらないこと

**このフェーズではやらない**

- 技術選定・ADR・コードを書くこと
- `CLAUDE.md`・`SESSION_STATE.md` の書き換え（`CLAUDE.md` の変更案は報告に文章で書く。`SESSION_STATE.md` の更新は検収後に別コミット）
- GitHub の設定変更。push（検収 PASS 後、主人の指示で行う）
- 前身 README への後継の一行
- `personalized-ai-agent-reference.md`（808行）の要約（出典として参照するのはよい）

**起きてはならない**

- 前身（`C:\work5\Kage-Shiki`）への書込
- 前身の `data/` と `.env` の読込（**機構なし**。検収でも見えない）
- 承認後の `goal.md` の書き換え
- 自分で検収を採点すること
- 成果コミットの amend・force push（検収の指摘は訂正コミットで直す）
