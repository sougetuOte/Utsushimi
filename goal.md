# goal.md — 段1 T2「人格の器」

**状態：承認済み（2026-09-23 主人）**
種別：feature（G0 あり）。材料は `docs/specs/stage1/`（tasks の T2・design §2・§4.1・§4.2）と ADR 0002・0003。モデルは Opus 5.5（D8）。
基準コミット（着手前）= `493d9ab`。前身 = `C:\work5\Kage-Shiki`（以下 `K/`）。

範囲：**根っこを読み、固め、守る。**T1 の本体に足す。中身は `tasks.md` の T2 の記述どおり：
`data/persona/core.md`・`style.md` の書式と検査（BH-25）、`persona_seal`（SHA-256）と手での変更の検出（BH-21）、
文脈の組み方（固定の指示・根っこ・見本までをキャッシュの区切りにし、その後ろに今の時刻と日付、今日の会話）。
加えて、起動したらキャラから最初の挨拶を1つ出す（Bug-7 の完了条件が「最初の挨拶」を見るため。前身のセッション開始の発言にあたる）。
前置きとして、T1 の確かめで `data/` に溜まった試しのデータを消す（2026-09-23 主人決定）。

- 確かめ用の人格は、Claude が手で書いた仮のファイルを `data/persona/` に置き、「この内容で固め直す」で固める（`tasks.md` の T2 の記述どおり）。
  **キャラの手触りはこのフェーズでは作り込まず、判定もしない**（本物の人格は T3 のキャラ作りで生まれる）。
- 必須の見出しは `docs/concept.md` §8.2 の「必須」（C1〜C6・C9・C11）と S1〜S7。任意（C7・C8・C10）は無くてよい。
  見出しがあって中身が空なのも壊れているとみなす。
- `persona_seal` に行が無い（まだ固めていない）ときも、手で変えられたときと同じ小窓で問う。
- 人格のファイルに書く口は作らない（`seal(draft)` の本体は T3）。固め直しは `persona_seal` に行を**足す**だけで、ファイルには触らない。

## 完了条件

1. **T1 の試しのデータを消した**：`data/utsushimi.db`（と `-wal`・`-shm`）・`data/logs/`・`data/smoke/t1/` を、T2 の確かめを始める前に
   **ごみ箱へ**移した（`data/config.toml` は既定と同じ中身なので残す）。
2. **固めた人格で話す（BH-24・BH-19・BH-01）**：固めた人格で実起動して10往復話し、返事に括弧書きのト書きが1つも無く、
   終了後に人格のファイル2つのハッシュが `persona_seal` の最後の行と同じだった。
3. **壊れていたら動き出さない（BH-25）**：`core.md` から C1 の見出しと中身を消して実起動すると、会話の窓は出ずに、
   どのファイルのどこ（ファイル名と見出し）が壊れているかを知らせる小窓が出て、閉じると数秒以内に本アプリのプロセスが残らなかった。
4. **手で変えたら気づく（BH-21）**：終了中に人格のファイルを手で1文字変えて実起動すると変更が知らされ、
   「このまま使う」を選ぶと固め直さずに会話の窓が出て次の実起動でもまた知らされ、
   「この内容で固め直す」を選ぶと次の実起動では知らされなかった。
5. **時刻を知っている（Bug-7）**：時間帯の違う2回に実起動し、それぞれ最初の挨拶がその時間帯に合っていた（**判定は主人**）。
   1回目は Claude が確かめの中で（2026-09-23 夜）、2回目は翌朝に主人がショートカットから起動して挨拶を読み、判定する（主人承認）。
6. **キャッシュが効く（ADR 0003）**：実起動した10往復のうち2往復目以降の呼び出しで、ログの `cache_read` が0より大きかった。
7. **根っこを書く口が無い（BH-19）**：`src/` に、`data/persona/` の下へ書き込むコードが無い。
8. **手順書（B-10・共通）**：`docs/smoke-test.md` に 2〜6 の確かめ方と、2つの小窓（壊れている／変えられた）の目視項目がある。
   その全項目を実起動で通した日付と結果が報告に書かれている。T1 の項目 S-1〜S-5 も同じ版で通し直した。
9. **目視（L-5・共通）**：実起動して2つの小窓と会話の窓を目で確かめ、はみ出し・重なり・操作できない部分が無かったことが報告に書かれ、
   それぞれのスクリーンショットがある。
10. **秘密が漏れない**：API キーがコミットにもログにも出ていない。人格のファイル・`data/` がコミットに無い。

### ログの語（検収はこの語で数える）

T1 の語（`start`・`send id=`・`reply id=`・`hide`/`show`・`shutdown begin`/`shutdown done`・`ERROR`）はそのまま。足すのは次。
本文は出さない（人格のファイルの中身もログに出さない）。

| 出来事 | 語 |
|---|---|
| 人格を読んだ | `persona ok core=<sha256> style=<sha256>` |
| 人格が壊れている | `persona broken file=<ファイル名> heading=<見出し> reason=<missing\|empty>`（壊れている所ごとに1行） |
| 固めた値と違う／まだ固めていない | `persona changed` ／ `persona unsealed` |
| 主人の選択 | `persona resealed core=<sha256> style=<sha256>` ／ `persona kept` |
| 最初の挨拶の保存 | `greet id=<番号>` |
| LLM の呼び出しごとの使用量 | `usage kind=<greet\|reply> input=<数> cache_read=<数> cache_write=<数> output=<数>` |
| 終了のときの人格のハッシュ | `persona at_exit core=<sha256> style=<sha256>` |

## 検証方法

検収役が見る。採点範囲は**本フェーズの成果コミット**（`493d9ab` より後で、報告に列挙したもの）に限る。
実起動の証拠は git の外にある：ログ `data/logs/utsushimi.log`、発言と封 `data/utsushimi.db`、スクリーンショット `data/smoke/t2/`。
**報告に、各完了条件を確かめた起動のプロセス番号と時刻を書く。**検収役はその番号でログを引き、`data/utsushimi.db` を読み取り専用で開いて突き合わせる。

| # | 完了条件 | 確かめ方 |
|---|---|---|
| V1 | 1 | `test ! -e data/smoke/t1`。ログの最初の行の時刻と `lifecycle` の最初の行の `started_at` が、どちらも本フェーズの G0 コミットの時刻より後。報告に、ごみ箱へ移したパスの一覧がある |
| V2 | 2 | 報告の起動のプロセス番号で、`send id=`・`reply id=` がそれぞれ10行以上、同じ番号の `ERROR` が0行。`utterances` のその番号の返事10件の本文に、正規表現 `[（(][^）)]*[）)]` の当たりが0件。その起動の `persona ok` と `persona at_exit` のハッシュが互いに同じで、`persona_seal` の最後の行（その起動より前に足された行）とも同じ。報告に、終了後に `Get-FileHash`（SHA256）で取った2つの値があり、それも同じ |
| V3 | 3 | 報告の起動に `persona broken file=core.md heading=C1` があり、`show via=start` が無く、`shutdown done` がある。報告にその小窓のスクリーンショット（ファイル名と C1 が読める）と、閉じた後の `tasklist` の出力（本アプリの `pythonw.exe` が無い）がある |
| V4 | 4 | 報告の3つの起動が順に：(a) `persona changed` → `persona kept` → `show via=start`、(b) `persona changed` → `persona resealed`、(c) `persona changed` も `persona unsealed` も無く `persona ok`。(a) と (b) のあいだで `persona_seal` の行が増えていない。(b) の後に増えた行のハッシュが (b) の `persona resealed` と同じ |
| V5 | 5 | 報告の2つの起動の `greet id=` の時刻が、`05:00〜10:59`・`11:00〜16:59`・`17:00〜04:59` の区分で違う区分にある。`utterances` のその2件の本文が報告に写してあり、報告に**主人の判定（可／不可）と日付**が書かれている |
| V6 | 6 | V2 の起動で、`usage kind=reply` の行が10行以上あり、2行目以降の `cache_read=` がすべて0より大きい |
| V7 | 7 | `grep -rnE "write_text\|write_bytes\|open\(.*['\"][wax]" src/` の当たりが、`data/persona` を指していない（当たった行を検収役が読んで確かめる）。`grep -rn "persona" src/` に `data/persona` へ書く処理が無い |
| V8 | 8 | `docs/smoke-test.md` に T2 の項目（2〜6 にあたるもの）と、2つの小窓の目視項目がある。報告に、S-1〜S-5 と T2 の全項目の実施日と結果（可／不可）がある |
| V9 | 9 | `data/smoke/t2/` に、壊れているときの小窓・変えられたときの小窓・会話の窓のスクリーンショットが1枚以上ずつある（検収役が画像を見て、はみ出し・重なりが無いことを確かめる）。報告に目視の結果がある |
| V10 | 10 | `git log -p 493d9ab..<最後の成果コミット>` を `grep -E 'sk-ant-[A-Za-z0-9]'` で見て0件。`data/logs/utsushimi.log` に `sk-ant` が0件。成果コミットに `.env`・`data/`・`.venv/` のパスが無い |
| V11 | 範囲 | `git diff --name-only 493d9ab..<最後の成果コミット>` が、`goal.md`・`src/`・`docs/smoke-test.md`・`README.md`・`config.default.toml` の下だけ |
| V12 | やらないこと | 前身に書き込んでいない：`git -C C:\work5\Kage-Shiki rev-parse --short HEAD` が `39b030f` |
| V13 | 品質 | 成果の Markdown に制御文字 `[\x00-\x08\x0b\x0c\x0e-\x1f]` が無い |

時間で壊れないように、証拠は**足すだけの物**（ログ・`utterances`・`persona_seal`）と、報告に書き写した値で見る。
「今の人格のファイルのハッシュ」「今プロセスが無いこと」のような、検収の時点で変わる値は物差しにしない。

## G1 の事前承認（この goal の承認に含む）

- T1 の試しのデータ（完了条件1のパス）を**ごみ箱へ移す**こと（完全な削除はしない。ごみ箱を空にするかは主人が決める）。
- `data/persona/` に確かめ用の人格のファイルを置き、確かめのために手で書き換えること（C1 を消す・1文字変える・戻す）。
- Anthropic API を実際に呼ぶこと（`.env` のキー。`claude-opus-5-5`。確かめに要る分だけ、目安40回以内）。
- 確かめの間、合成入力で主人の PC のマウスとキーボードを動かすこと（**主人に PC から離れてもらう**）。
  スクリーンショットは本アプリの窓の範囲だけを撮る。

## やらないこと

**このフェーズではやらない**

- キャラ作り・`seal(draft)`・`origin.md`（T3）。主人について・関係の変化・点検の文脈（T4・T6。文脈の 3・4 と点検の直しは空のまま）
- 文脈の上限と削る順（T4。今日の会話は T1 と同じく直近の発言を入れる）
- 確かめ用の人格の手触りを作り込むこと・判定すること
- 自動テストを書くこと（完了は実起動で数える。確かめ用の使い捨てスクリプトはスクラッチに置き、コミットしない）
- T2 の確かめで溜まる `data/` の中身（仮の人格・会話）を消すこと（T3 の G0 で扱う。T3 は人格が無い状態から始まるため）
- `CLAUDE.md`・`SESSION_STATE.md`・`docs/specs/` の書き換え（`SESSION_STATE.md` と `tasks.md` の ☑ は検収後に別コミット）
- push（検収 PASS 後、主人の指示で行う）

**起きてはならない**

- 前身（`C:\work5\Kage-Shiki`）への書込。前身の `data/` と `.env` の読込（**機構なし**。検収でも見えない）
- API キー・`.env`・`data/`・`.venv/` のコミット。キーや人格のファイルの中身をログや報告に書くこと
- `data/` の中身を完全に削除すること（ごみ箱を経ない削除）
- 主人に求めずに行うグローバル導入・処理系のインストール
- 承認後の `goal.md` の書き換え
- 自分で検収を採点すること
- 成果コミットの amend・force push（検収の指摘は訂正コミットで直す）
