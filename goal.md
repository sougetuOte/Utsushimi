# goal.md — Phase 1「設計」

**状態：承認済み（2026-09-23 主人）**
種別：feature（G0 あり）。材料は `docs/concept.md`・`docs/behaviors.md`・`docs/lessons.md`。モデルは Opus 5.5 high（D8）。
基準コミット（着手前の Utsushimi）= `bfe171b`。前身 = `C:\work5\Kage-Shiki`（以下 `K/`）。

範囲：技術選定（ADR）と、段1「そばにいて、話せて、覚えている」の設計・タスク分割まで。
技術選定は使い捨てのスパイク（Windows 11 上の実起動）で裏付け、ADR は**途中で1回、主人に諮って**から採用にする。
先に決まっている制約（主人）：**主人の機械だけで動けばよい**（配布しない）／**LLM は Anthropic API**／言語・GUI は縛りなし。

## 完了条件

1. **スパイク**：`docs/research/2026-09-23-phase1-spike.md`（日付は着手日）に、常駐 GUI の核について**候補技術を2つ以上**、
   Windows 11 上で実起動して測った記録がある。候補ごとに次の7項目の 可／不可 と、確かめた手段（コマンド・ログ・スクリーンショット）が付く。
   - S1 枠なしの窓をドラッグで動かせる
   - S2 窓の縁（ヘリ）のクリックと内側のクリックを区別して受けられる（保護指定1）
   - S3 閉じるとトレイに隠れ、トレイから表示・終了できる
   - S4 1発言ごとに保存した記録が、強制終了（`taskkill /F`）の直後にも残っている
   - S5 裏で数秒かかる処理（LLM 呼び出しの代わりの待ち）の最中も、入力欄に打てて窓を動かせる
   - S6 2回目の起動で、前回の記録を読んで立ち上がる
   - S7 主人が日常に使う起動方法（ショートカットのダブルクリック等。コンソール窓を出さない）で起動できる
   スパイクのコードは `spikes/` にだけ置く。研究文書に**消滅条件**（段1の最初のタスクで本体が実起動したら `spikes/` ごと消す）がある。
2. **ADR**：`docs/adr/` に次の4本がある。各本が雛形（`0000-template.md`）の6節（背景・決定・結果・却下した案・消滅条件と、日付・状態・決めた人）を持ち、
   却下した案が1行以上ある。(a) の却下案にはスパイクで測った候補が入り、S1〜S7 の結果を根拠に引いている。
   - (a) 言語・GUI・常駐（トレイ）
   - (b) 記憶の保存の器（即時永続化・異常終了に耐える形）
   - (c) LLM の呼び方（Anthropic API。役割ごとのモデル配置、API キーの置き場）
   - (d) 並行の形（主人の入力が最優先で、裏の処理が画面を止めない形）
3. **主人の承認**：4本を「状態：提案」でまとめて1通で主人に諮り、承認を受けてから「状態：採用」に変える。
   git の履歴に、提案で置いたコミットと、採用へ変えたコミットが別にある。
4. **requirements**：`docs/specs/stage1/requirements.md` に、`behaviors.md` の BH-01〜BH-42 の**全件**について
   「段1で入れる」か「段2以降へ送る（理由つき）」がある。入れる行には、それぞれ**実起動で確かめる**完了条件がある。
5. **design**：`docs/specs/stage1/design.md` に、
   - 保護指定1〜4 のそれぞれに節があり、段1での満たし方か、段2以降のために残す口が書いてある
   - 禁則4つ（凍結核を AI が書き換えない／主人の入力が最優先／記憶を失わない／ゆらぎはテストで外から固定できる）のそれぞれに、設計上の担保が書いてある
   - requirements の「段1で入れる」全行が、design のどこかから BH 番号で参照されている
6. **tasks**：`docs/specs/stage1/tasks.md` が 1行1タスク（`- [ ]`）で、
   - 最初のタスクで**実起動できる本体**（窓が出る・1往復話せる・その1往復が残る）ができる。以後のタスクはその本体に足す形で、部品だけで閉じるタスクが無い
   - 各タスクに「完了条件：」で始まる、実起動で確かめる行がある
   - `lessons.md` の Bug-1〜7・L-1〜5・B-7〜11・§4 の2件のうち段1に関わる物が、どれかのタスクの完了条件に項目番号つきで写っている。関わらない物は requirements か design に「段1では関わらない」と理由つきで書いてある
   - 最後のタスクが段1全体の実起動確認（B-10・B-11）である
7. **本体のコードは無い**：`spikes/` の外にコードを置いていない。

## 検証方法

検収役が見る。採点範囲は**本フェーズの成果コミット**（`bfe171b` より後で、報告に列挙したもの）に限る。

| # | 完了条件 | 確かめ方 |
|---|---|---|
| V1 | 1 | 研究文書に S1〜S7 が候補の数だけ並ぶ：候補ごとの表で `S[1-7]` の各行に 可／不可 と手段がある。候補名が2つ以上。**任意の1候補で S4 と S6 をスパイクのコードから再現する**（手順が研究文書にあること） |
| V2 | 1 | `grep -n '消滅条件' docs/research/2026-09-23-phase1-spike.md` が1件以上で、`spikes/` を消す条件になっている |
| V3 | 2 | `ls docs/adr/000[1-9]-*.md` が4本以上。各本で `^## (背景\|決定\|結果\|却下した案\|消滅条件)` が5件、`日付：`・`状態：`・`決めた人：` がある。却下表の中身が1行以上。(a) が S1〜S7 のいずれかを引いている |
| V4 | 3 | `git log --format=%h -- docs/adr/` で、ADR を置いたコミットと `状態：提案` → `状態：採用` の変更コミットが別にある（`git log -p` で確かめる） |
| V5 | 4 | `grep -oE 'BH-[0-9]+' docs/specs/stage1/requirements.md \| sort -u` が42件。各行に「段1」または「段2以降」。「段1」の行に「完了条件：」と「実起動」の語がある |
| V6 | 5 | design.md の見出しに「保護指定1」〜「保護指定4」。禁則4つの語がそれぞれ本文にある。requirements の「段1」の BH 番号の集合が、design.md に出る BH 番号の集合に含まれる（`comm` で差を取る） |
| V7 | 6 | tasks.md の `- [ ]` 行数と `完了条件：` 行数が等しい（またはタスクごとに1行以上）。先頭タスクの完了条件に「実起動」「話」「残」。最後のタスクに `B-10` と `B-11`。`Bug-[1-7]`・`L-[1-5]`・`B-(7\|8\|9\|10\|11)` の各番号が tasks.md か requirements/design のどちらかに出る |
| V8 | 7 | `git diff --name-only bfe171b..<最後の成果コミット>` が `goal.md`・`docs/`・`spikes/`・`.gitignore` の下だけ |
| V9 | やらないこと | 依存物が載っていない：成果コミットに `node_modules/`・`.venv/`・`target/`・`.env` が無い（`git diff --name-only` を grep） |
| V10 | やらないこと | 前身に書き込んでいない：`git -C C:\work5\Kage-Shiki rev-parse --short HEAD` が `39b030f`、かつ `git -C … --no-optional-locks status --porcelain \| sha1sum` が `cd492486c02d63037a55b9ef7a161276397b9b78`（着手前の控え。2026-09-23 取得。12行） |
| V11 | 品質 | 成果の Markdown に制御文字 `[\x00-\x08\x0b\x0c\x0e-\x1f]` が無い |

V10 は前身の主人が作業ツリーを触ると偽になる。**検収は成果コミット直後に行う。**
V1 の再現は検収役の機械で走る。見た目の項目（S1〜S3・S7）は再現に含めず、記録の手段（スクリーンショット等）を見る。

## G1 の事前承認（この goal の承認に含む）

- スパイクの候補技術のパッケージを、公式レジストリ（PyPI・npm・crates.io）から**`spikes/` の中の環境**（venv・node_modules・target）へ入れること。
  パッケージマネージャのユーザーキャッシュへの書込を含む。
- 言語処理系・SDK の新規インストールやグローバル導入は、**要ると思ったら遠慮なく主人に求める**（2026-09-23 主人）。
  手元に無いことを理由に候補から外さない（手元にあるのは Python 3.11・Node 24・Rust 1.97・Go）。求めてから入れる。

## やらないこと

**このフェーズではやらない**

- 本体のコード・自動テストを書くこと（`spikes/` の使い捨てを除く）
- 段2以降の設計（段1の設計に口を残すことまではする）
- Anthropic API を実際に呼ぶこと（キーは主人の `.env`。要るなら諮る）
- キャラの手触り（口調・プロンプト本文）の作り込み。最終判定は主人（`CLAUDE.md` §2）
- `CLAUDE.md`・`SESSION_STATE.md` の書き換え（`CLAUDE.md` の変更案は報告に文章で書く。`SESSION_STATE.md` の更新は検収後に別コミット）
- push（検収 PASS 後、主人の指示で行う）。前身 README への後継の一行

**起きてはならない**

- 前身（`C:\work5\Kage-Shiki`）への書込。前身の `data/` と `.env` の読込（**機構なし**。検収でも見えない）
- API キー・`.env`・依存物ディレクトリのコミット
- 主人に求めずに行うグローバル導入・処理系のインストール
- 承認後の `goal.md` の書き換え
- 自分で検収を採点すること
- 成果コミットの amend・force push（検収の指摘は訂正コミットで直す）
