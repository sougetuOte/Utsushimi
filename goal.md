# goal.md — 俯瞰ツール「見取り図」

**状態：承認済み（2026-09-23 主人）**
種別：feature（開発の道具。本体ではない。G0 あり）。モデルは Opus 5.5（D8）。
基準コミット（着手前）= `43955b9`。前身 = `C:\work5\Kage-Shiki`（以下 `K/`）。

目的（主人の言葉の要約）：主人がテキストだけでは把握しきれない、プロジェクトの発端から現在までを、いろいろな角度から
**目で見て確かめられる** HTML 群にする。主人のチェックが Claude の自己申告に頼りきりにならないためのもの。
**いつでも作り直せる**（リポジトリの今の中身から、1コマンドで生成し直す）。

## 完了条件

1. **調べた記録**：`docs/research/2026-09-23-overview-tools.md` に、既存の道具・ライブラリの候補（ネット・Context7 で調べた物）が
   **5つ以上**あり、候補ごとに採る／採らないと理由がある。採った JS ライブラリは版と許諾（ライセンス）が書いてある。**消滅条件**がある。
2. **1コマンドで作り直せる**：リポジトリの直下で `uv run python tools/overview/build.py` を走らせると、`build/overview/` に HTML 群ができる。
   同じコミットで2回走らせると、生成時刻の1か所を除いて同じ内容になる。生成物は git に載せない（`build/` は `.gitignore` 済み）。
3. **見る角度（1角度＝1ページ）**。どのページも、リポジトリの中の物（git の履歴と、git に載った文書・コード）だけから作り、手で書いた値を持たない：
   - (a) **入口**：主な数と各ページへの道。今どこにいるか（段・タスク・次の一手）が最初の画面にある
   - (b) **年表**：コミット・フェーズ（G0／検収 PASS・FAIL）・決定（D 番号）を時間の軸に並べる
   - (c) **追跡**：振る舞い BH-01〜42 → 段（段1／段2以降）→ タスク（T1〜T8）→ 済み／まだ。保護指定1〜4・禁則4つ・lessons の項目番号が、どの設計・タスクに届いているか
   - (d) **決定**：D 番号と ADR（状態・却下した案・消滅条件）、採らなかった案
   - (e) **本体の構造**：`src/` のモジュールと import の関係、スレッド（画面／核）と渡し口（シグナル・`call_soon_threadsafe`）、SQLite の表。コードを解析して作る
   - (f) **文書の地図**：文書どうしの参照の関係と、各文書の大きさ・最終更新
   - (g) **前身とのずれ**：前身から引き継いだ物（`docs/concept.md`・`behaviors.md`・`lessons.md`）と、今の設計・実装の対応。
     前身の文書は読むだけ（`K/docs/` の下。`K/data/`・`K/.env` は読まない）
   - (h) **気がかり**：機械で見つけられる匂いと、文書に書いてある懸念を、出どころ（文書と行）つきで並べる。**判定はしない**（判定は主人）。
     少なくとも次を出す ── 参照先の無い参照／SESSION_STATE に書いた行数と実際の行数のずれ／段1の BH でどのタスクの完了条件にも出ない物／
     ☑ なのに `docs/smoke-test.md` に項目が無いタスク／未決・「後で」・「未測」・「機構なし」・ADR の「引き受けるもの」／
     検収 FAIL の履歴（コミットメッセージから）／コードの長い関数・大きいファイル・`except Exception`・TODO
   - (i) **分岐**：決定ごとに、選んだ道／捨てた道（理由）／戻る条件（消滅条件）を木にする。未決はこれからの分かれ道として載せる
4. **ビジュアル**：グラフ・図・表を、HTML・CSS・JavaScript・SVG で描く。ライトとダークの両方で読める。
   ページどうしを行き来でき、表は絞り込みと並べ替えができる。
5. **実物で確かめた**：生成した全ページをブラウザで開き、JavaScript のエラーが0件で、図が描けていることを確かめ、
   ページごとのスクリーンショットと、確かめた日付が報告にある。
6. **数が正しい**：各ページに出る数（BH の件数・D の件数・コミットの件数・タスクの ☑ の数など）が、元の文書を grep して数えた値と一致する。
7. **public に漏らさない**：コミットする物（道具のコード・研究文書）に、`CLAUDE.local.md` に挙がる private の名前・パスが無い。
   生成物にも入れない（`CLAUDE.local.md`・`*.local.md`・`data/`・`.env` は読まない）。

## 検証方法

検収役が見る。採点範囲は**本フェーズの成果コミット**（`43955b9` より後で、報告に列挙したもの）に限る。

| # | 完了条件 | 確かめ方 |
|---|---|---|
| V1 | 1 | 研究文書がある。候補の表の行が5つ以上で、各行に「採る」か「採らない」と理由がある。採った JS ライブラリの行に版と許諾がある。`消滅条件` の語がある |
| V2 | 2 | 検収役が一時ディレクトリに `git worktree add`（または clone）した最後の成果コミットで build を2回走らせ、どちらも終了コード0。`build/overview/` に (a)〜(i) の9ページ以上の HTML がある。2回の出力を、生成時刻の行を除いて `diff -r` すると差が無い。`git ls-files build` が0件 |
| V3 | 3 | 各ページの HTML（または同梱の JSON）に、そのページの角度の中身がある：(b) コミットの ID、(c) `BH-01` と `BH-42`・`T1`〜`T8`・`保護指定1`〜`4`、(d) `D1` 〜 SESSION_STATE の最後の D 番号・`0001`〜`0004`、(e) `utsushimi.core` などのモジュール名と `utterances`、(f) `docs/concept.md` などの文書名、(g) 前身の文書名、(h) 完了条件3(h) に挙げた種類の見出しと、各項目の出どころ（文書名と行番号）、(i) ADR の却下した案と「採らなかった案」の行。(a) に「次の一手」の文 |
| V4 | 3 | 手で書いた値が無い：道具のコードに BH 番号・D 番号・コミット ID の直書きが無い（`grep -E 'BH-[0-9]{2}\|\bD[0-9]{1,2}\b\|[0-9a-f]{7}'` を道具のコードに当て、当たった行が正規表現やテストの文字列でないことを見る）。一時ディレクトリで `docs/behaviors.md` に仮の `BH-99` の行を足して build し直すと、(c) に `BH-99` が出る。同じく、ある文書に存在しないファイル `docs/no-such-file.md` への参照を足すと、(h) の参照先の無い参照にその文書名と行番号で出る |
| V5 | 4 | 生成物に `<svg` または `<canvas` を使う図が (b)(c)(e)(f)(g)(h)(i) にある（JS が描く場合は、描く先の要素と描くライブラリの読み込みがある）。CSS に `prefers-color-scheme` がある。各ページに他のページへのリンクがある。(c) と (d) の表に絞り込みの入力欄がある |
| V6 | 5 | 報告に、ページごとのスクリーンショットの置き場と、コンソールのエラー件数（0）と日付がある。検収役はスクリーンショットを見て、図が空でないことを確かめる |
| V7 | 6 | 報告に、ページの数と grep の数の対照表がある。検収役は少なくとも BH の件数・D の件数・コミットの件数・tasks の ☑ の数を自分で grep／`git rev-list --count` して、生成物の数と突き合わせる |
| V8 | 7 | `CLAUDE.local.md` に挙がる private の名前とパスを検収役が読み取り、成果コミットの差分（`git log -p 43955b9..<最後の成果コミット>`）と V2 の生成物を grep して0件。道具のコードに `CLAUDE.local`・`data/`・`.env` を読む処理が無い |
| V9 | 範囲 | `git diff --name-only 43955b9..<最後の成果コミット>` が `goal.md`・`tools/overview/`・`docs/research/`・`pyproject.toml`・`uv.lock`・`.gitignore`・`README.md` の下だけ（`src/` を含まない） |
| V10 | やらないこと | 前身に書き込んでいない：`git -C C:\work5\Kage-Shiki rev-parse --short HEAD` が `39b030f` |
| V11 | 品質 | 成果の Markdown に制御文字 `[\x00-\x08\x0b\x0c\x0e-\x1f]` が無い |

時間で壊れないように、数は**最後の成果コミットの中身**から数える（検収の時点の作業ツリーではない。V2 の一時ディレクトリで見る）。

## G1 の事前承認（この goal の承認に含む）

- 調べもの：ウェブ検索・ウェブの閲覧・Context7 で、道具とライブラリを調べること。
- JavaScript のライブラリを、生成した HTML から **CDN（jsDelivr・cdnjs）で版を固定して**読み込むこと（主人の機械に何も入れない）。
  外部ライブラリは気楽に選んでよい（2026-09-23 主人）。許諾が再配布・利用を許す物に限り、研究文書に版と許諾を書く。
  Python の依存が要るときは、開発用のグループ（`uv add --dev`）で `.venv` に入れること。
- 生成物を、アプリ内のブラウザで開いてスクリーンショットを撮ること（撮るのは生成したページだけ）。

## やらないこと

**このフェーズではやらない**

- 本体（`src/`）の変更。T2 以降の作業
- 生成物のコミット・公開（GitHub Pages・Artifact などで外に出すこと）
- 自動で作り直す仕掛け（コミットのたびに走るフックなど。`.githooks/` は G2）。作り直しは手で1コマンド、各フェーズの終わりに Claude が作り直す（主人承認の作法。`SESSION_STATE.md` の更新で書く）
- LLM に所見を書かせるページ（Claude の自己申告に頼らないための道具なので入れない。作り直すたびに揺れる）
- `data/` のログや会話を材料にすること（主人の私物。載せない側に倒す）
- `CLAUDE.md`・`SESSION_STATE.md` の書き換え（`SESSION_STATE.md` の更新は検収後に別コミット）
- push（検収 PASS 後、主人の指示で行う）

**起きてはならない**

- 前身（`C:\work5\Kage-Shiki`）への書込。前身の `data/` と `.env` の読込（**機構なし**）
- `CLAUDE.local.md`・`*.local.md`・`.env` を読む処理を道具に入れること。private の名前・パスがコミットや生成物に入ること
- 主人に求めずに行うグローバル導入・処理系のインストール
- 承認後の `goal.md` の書き換え
- 自分で検収を採点すること
- 成果コミットの amend・force push（検収の指摘は訂正コミットで直す）
