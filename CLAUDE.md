# Utsushimi — CLAUDE.md

**変わらないものだけを置く。**今回のフェーズの契約は `goal.md`、現在地と決定は `SESSION_STATE.md`。
ここに書いたことを他の2つへ写さない。3箇所で同じものを保守しない。

---

## 0. 目的（最上位。すべての判断はここに従う）

> 人格を持ち、記憶を引き継ぐ、1対1の常駐デスクトップの相棒。
> 人格の根っこは最初に決めたら変わらない。根っこ以外が、共に過ごした歴史（エピソード）によって変わっていく。

"Not yet divine. Not yet free." ── 前身「影式（Kage-Shiki）」の副題を継ぐ。

**完成形は1対1の相棒である**（2026-09-23 主人決定）。AITuber（1対多の配信）は拡張口として残すが、
設計の制約は1対1の側から決める。両者は「作業の邪魔をしない」と「注意を集める」で逆を向くからである。

出典：`C:\work5\Kage-Shiki\docs\memos\方向性.md`（主人の言葉）、同 `docs\memos\middle-draft\04-unified-design.md`。

### 目的から導かれる禁則

- **人格の根っこ（凍結核）を AI が書き換えない。**凍結後に変えられるのは主人の手だけである。
- **作業の邪魔をしない。**主人の入力が常に最優先で、自律的なふるまいは入力が来たら即座に引く。
  自分から話しかける頻度は「うるさくない」側に倒す。
- **記憶を失わない。**会話は即時に永続化し、異常終了や電源断でも失わない。
  蓄積された記憶は関係の証であり、失えば取り返しがつかない。
- **同じ入力から同じキャラは生まれない。**生成時のゆらぎが唯一性をつくる。
  ただしテストでは、ゆらぎを外から固定して再現できるようにする。
- **迷ったら足さない。**できるだけシンプルに、できるだけテキスト系で。

### 保護指定（主人が明示した質。作り直しても満たす）

出典はすべて `方向性.md` と `docs\memos\2026-03-03-char.md`。

1. ウィンドウのヘリなどを突っつくと反応する。そこをボディの代わりにする
2. メインループは止まらない。幾つかの行動予定が回り、やる／やらないは行動予定の活性で制御する
3. 最初の人格や癖のうち、主人が書いた部分以外は、AI が調べながら作る。その過程のゆらぎが唯一無二にする
4. ユーザーが書ける部分と書けない部分を分ける

## 1. 何を作るか（概要）

Windows 11 で常駐する、テキスト主体のデスクトップの相棒。
前身は `C:\work5\Kage-Shiki`（Python。Phase 2b の途中で止まっている）。
**前身からはコンセプト・基本設計・完成形だけを引き継ぎ、コードは持ち込まない。**技術選定は決め直す。

前身は**読むだけ。改変しない。**何をどう抜き出すかは `docs/handoff/phase0-brief.md`。

## 2. 作法

- 回答・コメント・コミットメッセージは日本語。
- **キャラの手触り（人格らしさ・口調・話しかけの間）の最終判定は主人が行う。**
  AI の「自然になった」「人格が保たれている」という自己申告は根拠に採らない。
- **本リポジトリは public である。**API キーは `.env` に置き、git に載せない。
- **キャラと記憶のデータ（`data/`）は git に載せない。**前身の既存キャラ（`C:\work5\Kage-Shiki\data\`）は
  主人の私物であり、引っ越しを扱うフェーズ以外では読まない。
- 修正は差分編集。全文の書き直しはしない。

### 作業姿勢（Anthropic「Prompting Claude Fable 5」より）

タスクが要求する範囲を超えて、機能を足したり、リファクタリングしたり、抽象化を持ち込んだりしないこと。
バグ修正に周辺の整理は要らないし、一回きりの操作にヘルパー関数はたいてい要らない。
仮想的な将来の要件のために設計しないこと。うまく動く最も単純な方法を取る。
起こり得ない状況のためのエラー処理・フォールバック・バリデーションを足さないこと。
内部コードとフレームワークの保証は信頼する。検証はシステム境界（ユーザー入力・外部 API）でのみ行う。

作業中やテスト中に、既存のバグ・性能の懸念・タスクが触れていない振る舞いに気づいても、この変更では直さない・広げない
（依頼された振る舞いがそれ無しでは動かない場合を除く）。最後の報告に「後で」として書く。
テストをコミットするのは、タスクが求めたときか、このリポジトリが同種の変更にテストを持っているときだけ。
量は述べられた振る舞い1つにおよそ1本。確認用の使い捨てスクリプトを常設のテストにしない。
これは「足すな」であって「削れ」ではない。依頼された振る舞いは全部、完全に実装する。
（Anthropic「Prompting Claude Fable 5.1」の指針。**消滅条件**：次のモデル世代でこの段落を外して試し、
頼んでいない修正やテストの水増しが戻らなければ消す）

行動できるだけの情報が揃ったら、行動すること。
会話の中で既に確定した事実を導き直さない。主人が既に下した決定を蒸し返さない。
選択を迷っているなら、網羅的な一覧ではなく推奨を 1 つ出す。

進捗を報告する前に、各主張をこのセッションのツール結果と突き合わせること。
証拠を指し示せる作業だけを報告する。未検証のものは未検証だと明示する。
テストが失敗したなら、その出力とともにそう言う。手順を飛ばしたなら、飛ばしたと言う。

## 3. 運営

このプロジェクトは3ゲート（G0 ゴール合意 / G1 不可逆操作 / G2 統治自己改変）で回す。
**条文の場所・面接の入口・検収役の名前は `CLAUDE.local.md` にある**（git に載せない。主人のローカルにだけある）。
条文は写さず読みに行く。読めなければ主人に言う。

- 各フェーズは面接（G0）から入る。
- 検収は必ず検収役（評価器）に出す。**自分で採点しない。**
- **G2 の機構は第2層だけある** ── `.githooks/pre-commit`（＋ `git config core.hooksPath .githooks`）。
  第1層（`permissions.deny`）は無いので、作業ツリーへの書込は止まらない。止まるのは**コミットに載ること**である。
  **保護対象の正本はフックの判定式**（`PROTECTED_RE`）。ここに写さない。承認して通す手順はフックの先頭コメント。
  フックが見ない範囲で何が G2 かは `goal.md` の「やらないこと」に書く。本ファイルへの書込も G2 である。
- **モデル**：既定は Opus 5.5（設計を含む）。medium で詰まったら high へ上げる。
  high で同じ問題に2回つまずいたら Fable 5.1 へ切り替え、解決したら戻す。監督しない長時間走行も Fable。
  切り替えはセッションの頭で行う（途中で変えるとキャッシュが切れる）。
  配置の正本は、決めた時点で `docs/adr/` に置く（まだ無い）。

## 4. 持ち越さなかったもの

前身から、次は**意図的に持ち込んでいない**。

| 前身 | 理由 |
|---|---|
| `src/`・`tests/`（約2.6万行） | Haiku 4.5 前提で、統合（Wave 5）も済んでいない。新しい世代のモデルに作り直させる。前身のコードは読んで振る舞いを確かめるためだけに使う |
| LAM 資産（rules 13 / agents 9 / skills 18 / hooks 5、CLAUDE.md 234行） | 3ゲートの運営に置き換えた。ハーネスの部品はモデルにできないことの仮定を抱えており、世代が変われば外す |
| フェーズコマンドと多段の承認ゲート | G0 の面接1回と検収役の検収に畳んだ |
| MAGI / HGA / full-review / wave-plan | 要るようになったら、その時点のやり方で組む |
| 推奨モデル表（Sonnet 実装・Haiku 既定） | 世代が変わった。配置は決め直す |
| 詳細設計 d01〜d20 | 実装の詳細であり、技術選定が変われば失効する |
